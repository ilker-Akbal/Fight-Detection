[pytest]
testpaths = tests shared_inference/tests
markers =
    unit: fast isolated unit test
    integration: multi-component integration test
    django: Django request/database test using an isolated test database
    multiprocessing: controlled process lifecycle test
    ffmpeg: requires ffmpeg, ffprobe, and libx264

